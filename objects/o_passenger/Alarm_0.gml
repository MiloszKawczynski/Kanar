
// You can write your code in this editor

move_check_num = irandom_range(1,1000);
if(move_check_num<=move_check_success)
{
	enteredx+=irandom_range(-xrange,xrange);
	enteredy+=irandom_range(-yrange,yrange);
}
latency = irandom(5)
alarm[0] = (move_check_time+latency)*room_speed;